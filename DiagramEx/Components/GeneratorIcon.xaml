<UserControl x:Class="DiagramEx.Components.GeneratorIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:base="clr-namespace:DiagramEx.Components"
             mc:Ignorable="d">
    <Grid>
        <Path Data="M 0 6 Q 1.6 0 4 0 Q 6.4 0 8 6 Q 9.2 12 12 12 Q 14.4 12 16 6" 
              Stretch="Fill"
              StrokeThickness="{Binding Thickness, RelativeSource={RelativeSource AncestorType={x:Type base:GeneratorIcon}}, UpdateSourceTrigger=PropertyChanged}"
              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type base:GeneratorIcon}},UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
